# Tasks — deck-analysis-api

## 1. Contract

- [ ] 1.1 cortex-api: ADD "Deck analysis composition endpoint" + MODIFY closure (two → three routes)
- [ ] 1.2 gateway-api: MODIFY protected-proxy (analysis → cortex + scenario), closure (nineteen → twenty), CORS enumeration
- [ ] 1.3 `openspec validate deck-analysis-api` + validate-api-closure.sh pass

## 2. Archive

- [ ] 2.1 Archive once the cortex + gateway + app sibling slices are live-validated end-to-end
