# Tasks — oracle-api-hybrid-search

## 1. Contract

- [ ] 1.1 MODIFY oracle-api "Semantic rules search endpoint" (hybrid wording, relevance score, relaxed reformulation note, verbatim-phrase scenario)
- [ ] 1.2 `openspec validate oracle-api-hybrid-search` passes; validate-api-closure.sh unaffected (route set unchanged)

## 2. Archive

- [ ] 2.1 Archive once the sibling `rbrain-oracle` implementation change is validated live (verbatim-phrase scenario green against the synced corpus)
