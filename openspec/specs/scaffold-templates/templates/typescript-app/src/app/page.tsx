export default function Home() {
  return (
    <main>
      <h1>rbrain-${CONTEXT_NAME}</h1>
      <p>
        Placeholder homepage for the <code>${CONTEXT_NAME}</code> bounded context of the MTG R.brain
        platform. Replace this with the actual UI.
      </p>
    </main>
  );
}
