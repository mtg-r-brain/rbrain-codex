## 1. Hand-off

- [x] 1.1 Commit, push, and confirm CI on `rbrain-codex` stays green (spec-only change; validators untouched).
- [x] 1.2 Archive this change via `openspec archive card-search-api`. The ADDED requirements append to `openspec/specs/lexicon-api/spec.md` and the MODIFIED requirement replaces the existing route-count cap paragraph.
- [ ] 1.3 With the contract live, open `card-search` in `rbrain-lexicon` and proceed with the implementation slice (tsvector migration, query handler, integration tests).
