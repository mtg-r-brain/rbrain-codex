# Tasks — deck-analysis-api

## 1. Contract

- [x] 1.1 cortex-api: ADD "Deck analysis composition endpoint" + MODIFY closure (two → three routes)
- [x] 1.2 gateway-api: MODIFY protected-proxy (analysis → cortex + scenario), closure (nineteen → twenty), CORS enumeration
- [x] 1.3 `openspec validate deck-analysis-api` + validate-api-closure.sh pass

## 2. Archive

- [x] 2.1 Archive once the cortex + gateway + app sibling slices are live-validated end-to-end
